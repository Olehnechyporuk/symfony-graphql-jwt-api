import AuthForm from '@/components/AuthForm/AuthForm';
import {Component} from 'vue-property-decorator';

@Component({})
export default class Login extends AuthForm {
    private tests2: number = 3;
}
